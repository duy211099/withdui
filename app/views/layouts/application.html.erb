<!DOCTYPE html>
<html lang="en">
  <head>
    <title inertia>Withdui</title>
    <meta name="description" content="Withdui - A modern web application built with Rails and React">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#ffffff">
    <link rel="manifest" href="/manifest.webmanifest">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Be Vietnam Pro Font - Non-blocking async load -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@100;200;300;400;500;600;700;800;900&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet"></noscript>

    <%= vite_stylesheet_tag "application" %>
    <%= vite_react_refresh_tag %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag "inertia" %>
    <%= inertia_ssr_head %>
    <% favicon_path = Rails.root.join("public/images/favicon.ico") %>
    <% favicon_version = File.exist?(favicon_path) ? File.mtime(favicon_path).to_i : nil %>
    <%= favicon_link_tag "/images/favicon.ico#{favicon_version ? "?v=#{favicon_version}" : ""}" %>

    <style>
      /* Loading indicator - shown while JavaScript loads */
      #app-loading {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: white;
        z-index: 9999;
      }

      #app-loading .spinner {
        width: 48px;
        height: 48px;
        border: 4px solid #f3f3f3;
        border-top: 4px solid #3b82f6;
        border-radius: 50%;
        animation: spin 1s linear infinite;
      }

      @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }

      /* Hide loading indicator once React hydrates */
      #app:not(:empty) ~ #app-loading {
        display: none;
      }
    </style>
  </head>

  <body>
    <%= yield %>

    <!-- Loading indicator shown while JavaScript loads -->
    <div id="app-loading">
      <div class="spinner"></div>
    </div>

    <!-- Analytics scripts deferred to end of body for better performance -->
    <!-- Google tag (gtag.js) - Deferred loading -->
    <script>
      window.addEventListener('load', function() {
        var script = document.createElement('script');
        script.async = true;
        script.src = 'https://www.googletagmanager.com/gtag/js?id=G-HRCFSNY3QK';
        document.head.appendChild(script);

        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-HRCFSNY3QK');
      });
    </script>

    <!-- Microsoft Clarity tracking code - Deferred loading -->
    <script type="text/javascript">
      window.addEventListener('load', function() {
        (function(c,l,a,r,i,t,y){
          c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
          t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
          y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "skbp3huvfy");
      });
    </script>
  </body>
</html>
