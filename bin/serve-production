#!/usr/bin/env bash
# Run Rails with production-built assets (useful for testing production build locally)

set -e

echo "🏗️  Building production assets..."
bin/vite build

echo ""
echo "🚀 Starting Rails server with production assets..."
echo "   Visit: http://localhost:3000"
echo "   Press Ctrl+C to stop"
echo ""

# Run Rails server (will serve the built assets from public/vite-dev)
bin/rails server
